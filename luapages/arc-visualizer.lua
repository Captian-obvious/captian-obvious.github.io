local id = "" -- module id--
local createArc = require(id)
local maxPL = 0

function createCircle(pos,r,num)
    local angle_step = math.pi*2
    for i=1,num do
        math.cos(i)*(10)+x, math.sin(i)*(10)+y
    end
end
